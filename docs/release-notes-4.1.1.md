# Customize+ 4.1.1

## Fixes

- Fixed lock-only template rows being lost during runtime armature binding.
- Fixed lock-only template rows being omitted from grouped transform exports.
- Fixed IPC profile conversion dropping `LockState`.
- Improved template-copy isolation for mutable bone transforms.
